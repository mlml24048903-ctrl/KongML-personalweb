# Design — 孔米乐的玩趣工作台

这是全站统一的视觉规则。它借鉴互动个人网站的可探索结构，但拒绝程序员工具界面的冷硬感。

## Genre

Playful editorial：好玩、轻巧、有个人性；像一张正在被使用的创意桌面，而不是仪表盘、开发工具或手账模板。

## Macrostructure family

- 首页与屏幕外：保留既有电脑、文字、打印机、便签和鼠标坐标；只改变纸面、墨色、层次与小物件语言。
- 屏幕内：保留文件系统、窗口和代码模式逻辑；图形模式更像彩色资料夹，代码模式仍是终端。
- 个人页面：漂浮编辑台。每页只有一个主构图，其余内容以不同尺度、重叠和游离物件围绕主构图出现。

## Theme

- 米白色是页面纸面，不使用纯白。
- 墨绿色与近黑色承担正文、大色块和 CRT 环境。
- 荧光绿承担高能提示、可点击状态和短标签。
- 橙红承担动作、强调、手绘标记和局部物件。
- 同一视口中荧光绿与橙红都保持克制，不铺满大面积背景。

## Typography

- Display：中文宋体/衬线体，正常体；用于大标题与个人宣言。
- Body：清晰中文黑体；用于正文和操作。
- Label：等宽字体只用于编号、短标签与系统状态，不主导页面气质。

## Spacing and shape

- 使用 4px 基础间距令牌。
- 不构造整齐等宽卡片矩阵。
- 边界只在可点击对象和主信息面板出现；关联主要通过距离、重叠、方向和大小表达。
- 纸张保持轻微错位，但不再依赖胶带、撕边、邮戳和统一投影制造“拼贴”。

## Motion

- 页面级：真实彩色快照转为 WebGL 粒子，旧页沿流场瓦解，新页由下向上重新组成。
- 局部：物件仅使用短距离位移、轻微旋转与状态形变。
- 不使用弹簧回弹，不持续运行背景粒子。
- Reduced motion：不超过 150ms 的透明度切换。

## Three profile pages

- 关于我：一张主声明与一组围绕它漂浮的个人特质、坐标和长期母题；开放、松弛、可探索。
- 此刻：三个不同尺度的进行中主题沿一条松散路径展开，不像任务管理器。
- 联系我：大号邮箱与一条橙红动作轨迹为中心，内容更少、更有留白。

## What must remain

- 首页 slogan 文案。
- 电脑、主机、鼠标、打印机与便签的既有功能。
- 屏幕内外切换路径与凸面 CRT 效果。
- 现有中文内容与邮箱信息。

## What is explicitly avoided

- 开发者仪表盘和伪 IDE 气质。
- 重复卡片、完整边框网格和每块内容都被框住。
- 米色手账模板、成排胶带、撕纸与邮戳。
- 均匀圆点蒙版冒充粒子。

## Profile transition revision

- 清晰页面与粒子必须来自同一张页面快照、同一套采样网格和同一滚动坐标。文字与图像的清晰像素在交界处逐格产生缺口，被移除的同色像素同步成为粒子；禁止用 DOM 渐隐再叠加一层互不关联的粒子。

- 三个个人页面是同一张纵向长画布上的连续段落，滚轮直接移动画布；页面不得原地互相替换，也不得在停止滚动后自动归位或补完。
- 任意滚动位置都是合法且稳定的最终状态。反向滚动必须逐像素还原刚才经过的页面位置和粒子状态。
- 相邻页面的交界区域采用宽而不规则的真实采样粒子：旧页靠近底部的文字和图像逐渐分开，新页靠近顶部的粒子逐渐组成内容；清晰 DOM 与粒子层共享同一滚动坐标。
- 纸面背景保持连续，不参与密集粒子化；粒子只采样文字、图形、色块和明显纹理，位移保持短促克制。
- 顶部入口是纵向画布的定位器，点击后平滑滚动到对应页面；移动端保留原生长页面滚动，并在性能不足时优先保证内容连续与可读。

## Paper turn revision

- 页面级粒子效果全部弃用，由纸张揉皱翻页取代；此前 Profile transition revision 中涉及粒子瓦解、粒子重组的规则不再生效。
- 旧页面是一张带内容的完整纸张。翻页时从下方向上逐步压缩、卷起，文字、图像与纸面一起发生网格形变，不允许用独立纹理遮住内容来冒充褶皱。
- 褶皱以细密斜向折痕为主，加入少量较深的明暗折脊；总体比完全揉皱的废纸更平整，必须保持页面内容可辨。
- 下一页平整铺在旧页下方，随旧页向上卷起而露出。滚动可停在任意进度，反向滚动逐像素还原，不自动补完或吸附。
- 移动端优先连续、可读与顺滑；性能不足时保留原生长页滚动，不强制运行 WebGL 纸张网格。
## 2026-08-12 · 正俯视二维长桌

- 已先恢复远程 `main` 基线；此前三维桌面实验保存在本地 `stash@{0}`，没有删除，也没有推送。
- 三个顶部页面现在共享一张横向连续的二维木桌，以 100vw 为一个工作区，点击导航时整张桌面横向平移。
- 视角严格为正俯视，布局参考工作桌常见秩序：显示器靠上、键盘居中、鼠标在侧，页面主题物件分布于左右两翼。
- 所有桌面物件均以项目内 HTML/CSS 原创建模，没有使用带水印参考图片。

### 精细 CSS 长桌

- 已删除三张独立生成场景图，桌面重新由一张跨越全部区域的 CSS 木纹长桌承载，不再存在图片接缝。
- “关于我”使用首页设备的正上方对应：CRT 显示器、键盘、主机、鼠标、打印机、垃圾桶和便签。
- “此刻”是研究工作区，仅有笔记本、资料、照片、耳机、纸笔、胶带与茶杯，不重复电脑设备。
- “联系我”是通信工作区，以信封、厚卡纸、钢笔、邮票、电话、印章和通讯录为主体，不重复电脑设备。
- 物件使用渐变、内外阴影、侧面厚度、接缝和小型结构件区分旧塑料、玻璃、纸张、金属、陶瓷与皮革，同时沿用首页的米白、墨绿、珊瑚橙和荧光黄绿色。
- “关于我”的主要设备采用严格的正交俯视规则：倾斜屏幕以梯形呈现，打印机只显示顶盖和进出纸结构，立式主机只显示顶部风扇与接口；垃圾桶改为带翻盖投入口的小型桌面垃圾桶。
